allprojects {
    repositories {
        maven { url 'https://jitpack.io' }
    }
}

dependencies {
    implementation 'com.github.krem06:ESCPOS-ThermalPrinter-Android:master-SNAPSHOT', {
        changing = true
    }
}
